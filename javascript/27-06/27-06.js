/* Javascritp Object.getOwnPropertyNames() Method */

// The Object.getOwnPropertyNames() static method returns an array of all properties (including non-enumerable properties except for those which use Symbol) found directly in a given object.

// Object.getOwnPropertyNames(obj)

// {
//    let Obj = {firstName:'Gurang' , lastName:'Jogani' , age:26 , proffesion:'Developer'} 

//    let GetOwnPropertiesNames = Object.getOwnPropertyNames(Obj)

//    console.log(GetOwnPropertiesNames);
// }

/* Javascript Object.hasOwnProperty() Method */


// The hasOwnProperty() method of Object instances returns a boolean indicating whether this object has the specified property as its own property (as opposed to inheriting it).

// hasOwnProperty(prop)


{
    // let Obj1 = {}

    // Obj1.firstName = 'SkillQode'

    // console.log(Obj1);

    // let hasprops1 = Obj1.hasOwnProperty('FirstName')
    // let hasprops2 = Obj1.hasOwnProperty('firstName')

    // console.log(hasprops1);
    // console.log(hasprops2);
}

/* Javascript Object.values() Method */

// The Object.values() static method returns an array of a given object's own enumerable string-keyed property values.

// Object.values(obj)


{
    // let Obj = {
    //     name:'kiran',
    //     age:25,
    //     gender:'female'
    // }

    // let Values = Object.values(Obj)
    // let getOwn = Object.getOwnPropertyNames(Obj)

    // console.log(Values);
    // console.log(getOwn);
}

/* Javascript Object.keys() Method */

// The Object.keys() static method returns an array of a given object's own enumerable string-keyed property names.

// Object.keys(obj)

// {
//       let Obj = {
//         name:'kiran',
//         age:25,
//         gender:'female'
//     }

//     let Keys = Object.keys(Obj)

//     console.log(Keys);
// }

/* Javascript Object.entries() Method */

// The Object.entries() static method returns an array of a given object's own enumerable string-keyed property key-value pairs.

// Object.entries(obj)

{
    // const object1 = {
    //     a: 'somestring',
    //     b: 42,
    //     c:'skillqode'
    //   };

    // for(let [key , value] of Object.entries(object1)){
    //     console.log(`this object key is ${key} and value is ${value}`);
    // }
}







